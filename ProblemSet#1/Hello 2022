#include <stdio.h>
#include <cs50.h>
int main(void)
{
    //To get the user's name
    string name = get_string("What is your name? ");
    //Welcome address to user
    printf("hello, %s\n", name);
}
